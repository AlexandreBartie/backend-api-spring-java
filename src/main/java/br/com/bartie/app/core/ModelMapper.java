package br.com.bartie.app.core;

// import java.util.List;

// import br.com.bartie.app.serialization.DataMapper;

public class ModelMapper<T, DTO extends ModelDTO<DTO>> {

    // private DataMapper<T, DTO> mapper = new DataMapper<T, DTO>();
    // private DataMapper<T, DTO> mapper = new DataMapper<T, DTO>();

    // public T parse(DTO source) {
    //     return mapper.parse(source);
    // }

    // public List<T> parse(List<DTO> source) {
    //     return mapper.parse(source);
    // }

    // public DTO parseDTO(T source)
    // {
    //     return mapper.parseDTO(source);
    // }

    // public List<DTO> parseDTO(List<T> source) {
    //     return mapper.parseDTO(source);
    // }

}